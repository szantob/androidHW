<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.mit.inf.modes3dashboard">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RedButtonActivity"
            android:label="@string/title_activity_red_button"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NewElementActivity"
            android:label="@string/title_activity_new_element"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".service.MQTTIntentService"
            android:exported="false"></service>
    </application>

</manifest>